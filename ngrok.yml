version: "2"
authtoken: YOUR_NGROK_AUTHTOKEN # Replace with your ngrok auth token
tunnels:
  frontend:
    addr: 5173
    proto: http
    bind_tls: true
  backend:
    addr: 8000
    proto: http
    bind_tls: true
